
#TEST Mock key
Bước 1 — chạy RsaKeyGenerator
private_key.pem
public_key.pem
Bước 2 — copy key vào project
Bước 3 — chạy app Spring Boot

* Cực kỳ quan trọng
=> RsaKeyGenerator:
    + KHÔNG chạy cùng app
    + KHÔNG phải service
    + KHÔNG inject vào Spring

#LIVE
Generate bằng OpenSSL: Chạy trong terminal
1. Tạo private key
openssl genpkey -algorithm RSA -out private_key.pem -pkeyopt rsa_keygen_bits:2048
2. Tạo public key
openssl rsa -pubout -in private_key.pem -out public_key.pem